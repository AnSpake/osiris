#include <iostream>

int main(int argc, char **)
{
    if (argc != 3)
    {
        std::cerr << "Usage: ./server IP PORT\n";
        return 1;
    }

    // create server socket
    // server loop
    // close socket

    return 0;
}
